# 学习笔记规范（NOTES-GUIDE）

> 用途：规定本课程各章节 `my-note.md` 学习笔记的生成要求与统一风格。
> 今后要求"生成 / 更新某章学习笔记"时，一律遵循本规范，保证各章笔记风格一致。
> 适用文件：`<章节目录>/my-note.md`（如 `06-building-trustworthy-agents/my-note.md`）。

---

## 1. 总原则

1. **不是翻译，是重构**：不得逐段复述或翻译教程原文；只保留"教程讲了什么"之上的**结论、机制、权衡与判断**，用自己的话重写。
2. **只留核心**：以该章 README 与 `code_samples/` 为准，提炼最核心的内容；可删的废话一律删，宁短勿长。
3. **准确优先**：所有事实、API、代码片段必须与该章教程/示例代码一致；不确定就回去核对，不臆造。
4. **面向工程师**：讲"为什么/怎么选/边界在哪"，而不是"教程第三步说了什么"。
5. **可跨章串联**：每章都要有"现实映射"，说明它和前后章节的关系。

---

## 2. 文件结构模板（自上而下）

```markdown
# 第 N 章学习笔记：<中文标题>（<English Title>）

> 本章目标：<一句话，说明这一章要建立什么认知>
> 核心一句话：**<全章最凝练的结论，含关键取舍>**

---

## 1. <核心认知 / 机制>
...
## k. 踩坑 & 易错点
## k+1. <选型 / 应用>心法
## k+2. 现实映射
```

固定要求：

- **开头固定两行引用块**："本章目标" + "核心一句话"（核心一句话用 `**加粗**`）。
- **正文分节编号**：`## 1.` 起顺序编号。
- **结尾固定三节**（顺序不可变）：
  1. `## x. 踩坑 & 易错点`（列表）
  2. `## x. 选型心法` 或 `## x. 应用心法`（列表）
  3. `## x. 现实映射`（列表，串联前后章节与本仓库代码）
- 章节之间用 `---` 分隔。

---

## 3. 写作风格

- **中文为主，术语保留英文原词**（如 agent、tool、thread、RAG、MAF），首次出现可加中文解释。
- **深挖用拓展块**：教程没展开的"为什么/历史/对比/批判性阅读"统一放进 `> 拓展：...` 引用块，一条一个洞见，不堆砌。
- **对比用表格**：只在需要横向对比时使用（如 SDK vs 平台、传统 RAG vs Agentic RAG），列数不宜多。
- **代码只留关键片段**：只贴能说明机制的几行，并标注"这段在演示什么"；不整段搬运示例。
- **批判性阅读**：教程里的"简化演示"与"生产写法"若有差距，要明确指出（如"手动串接 ≠ 编排"）。
- **避免空话**：不写"本章很重要""要重视安全"这类无信息量句子。

---

## 4. 篇幅与取舍

- **单章目标长度约 70~120 行**（含表格与拓展块）；内容极多的章节可略超，但不得回到逐段翻译式的长文。
- 取舍优先级：核心机制 > 关键权衡/选型 > 踩坑 > 拓展洞见 > 示例细节。
- 同一洞见只出现一次；不重复教程已明说的东西。

---

## 5. 生成后自检清单

- [ ] 开头两行引用块齐全，且"核心一句话"确实概括全章。
- [ ] 全篇无成段翻译；读起来是"我的理解"而非"教程目录"。
- [ ] 事实/API/文件名与本章教程、示例代码一致。
- [ ] 结尾三节（踩坑 / 心法 / 现实映射）齐全且顺序正确。
- [ ] 现实映射已关联到本仓库的具体章节与代码文件。
- [ ] 全篇控制在目标篇幅内，无冗余重复。

---

## 6. 参考样例

- `01-intro-to-ai-agents/my-note.md`
- `02-explore-agentic-frameworks/my-note.md`
- `03-agentic-design-patterns/my-note.md`
- `04-tool-use/my-note.md`
- `05-agentic-rag/my-note.md`
- `06-building-trustworthy-agents/my-note.md`
- `07-planning-design/my-note.md`
